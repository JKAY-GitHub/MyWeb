
to run locally:
http://localhost:1313/

to start the server
../hugo server


github site:
https://github.com/JKAY-GitHub/JKAY-GitHub.github.io/tree/master

30 May 2021 - note that there was a problem wth the date set on the pubs-all....


edit the content

run "hugo -d ~/Dropbox/JKAY-GitHub.github.io" in top level

in directory ~/Dropbox/JKAY-GitHub.github.io do the following:

git pull
git commit -a
git push


---------------------------------

The script "doall" does those steps with confirmation at each step.


---------------------------------
Notes:
to get the password helper:
  git credential-osxkeychain

to use the password helper:
  git config --global credential.helper osxkeychain


